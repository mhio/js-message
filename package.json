{
  "name": "@mh/api-message",
  "version": "0.0.2",
  "description": "API Message Class",
  "main": "index.js",
  "repository": "https://github.com/mh/node-api-message",
  "author": "MH",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "mocha test/unit test/int",
    "test:unit": "mocha test/int",
    "test:int": "mocha test/unit",
    "test:coverage": "nyc _mocha test/unit test/int && nyc report -r html && nyc check-coverage",
    "test:watch": "nodemon -x mocha test/unit test/int",
    "release:test": "set -uex; yarn test; npm version prerelease; npm publish --tag testing",
    "release:patch": "set -uex; rm -rf node_modules; yarn; yarn run test:coverage; npm version patch; git push; npm publish; git push --tags"
  },
  "dependencies": {
    "base62-random": "^0.3.5",
    "chai": "^4.1.2",
    "mocha": "^4.0.1"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "nyc": "^11.3.0"
  },
  "nyc": {
    "per-file": true,
    "lines": 80,
    "statements": 80,
    "functions": 80,
    "branches": 80
  }
}
